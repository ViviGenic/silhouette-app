import React from "react";


// Plug in 
const Button = (props) => {



    return(
        <div>
            <button onClick={this.props.onClick}>{this.props.text}</button>
        </div>
        )
}